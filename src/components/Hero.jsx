import './Hero.css'; // Import the CSS file

const Hero = () => {
    return (
        <section className="hero-container">
            <div className="hero-title">
                Pursue React Development Skills
            </div>
            <div className="hero-subtitle">
                Identify the React role that complements your skill set
            </div>
        </section>
    )
}

export default Hero;
